<div><%= form_for [@post.user, @post], :url => {:action => "update", :id => @post}, :method => "patch"  do |f| %>  <% if post.errors.any? %></div>

    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <p><%= f.text_field :title %><%= label(:title, "title") %></p>
    <p><%= f.text_area :content %><%= label(:content, "content") %></p>
    <p><%= f.submit %></p>
  </div>
<% end %>
